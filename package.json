{
  "name": "cortex-monorepo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/dashboard",
    "packages/sdk"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
